<?xml version="1.0"?>
<TeamMentor_Article Metadata_Hash="0" Content_Hash="0">
  <Metadata>
    <Id>00000000-0000-0000-0000-000000924939</Id>
    <Library_Id>f798e6e6-1732-4172-9c47-9621d914efcd</Library_Id>
    <Title>Check Access Control Permissions When Referencing Objects</Title>
    <Category>Authorization</Category>
    <Phase>Implementation</Phase>
    <Technology> Any</Technology>
    <Type>Principle</Type>
    <DirectLink>Check Access Control Permissions When Referencing Objects</DirectLink>
    <Author />
    <Priority />
    <Status />
  </Metadata>
  <Content Sanitized="false" DataType="html">
    <Data><![CDATA[<h1>What to Do</h1>
  <p>Check access control permissions whenever performing direct object references.</p>
  <h1>Why</h1>
  <p>Using direct object references may expose sensitive data to attackers. Direct object references often expose sensitive data, such as files, database entries, or personal information. Direct object reference attacks are very simple to execute and have been used in high profile, low-skill attacks. Checking access control permissions whenever referencing objects helps prevent direct object reference vulnerabilities.</p>
  <h1>How</h1>
  <p>To check access control permissions whenever performing direct object references:</p>
  <ol>
    <li>
      <p>
        <strong>Identify pages that use direct object references.</strong> Make a list of all pages in the application. Go through the list and identify all pages that use indirect object references by examining each page.</p>
    </li>
    <li>
      <p>
        <strong>Identify code that references objects.</strong> For each page that uses indirect object references, identify the code that references objects.</p>
    </li>
    <li>
      <p>
        <strong>Check access controls.</strong> Add code that performs access control checks to code that performs object references.</p>
    </li>
  </ol>
  <ul>
  </ul>]]></Data>
  </Content>
</TeamMentor_Article>